# 运行指南（第一次跑项目，照着做就行）

## 第一步：确认文件夹结构

现在项目拆成了4个py文件，确保文件夹结构长这样：

```
agent_project/
├── main.py          # 主入口，FastAPI应用和路由
├── agents.py         # 所有Agent的实现（Collector/Analyzer/Formatter/Diagnostic）
├── models.py          # 数据模型定义
├── storage.py         # 数据持久化存储逻辑
├── requirements.txt
├── .env（自己创建，第二步会说）
└── static/
    └── index.html
```

## 第二步：填入你的 API Key

1. 如果还没有`.env`文件，新建一个`.env`文件（跟`main.py`同级）
2. 用记事本打开`.env`，写入一行：
   ```
   DEEPSEEK_API_KEY=你的key
   ```
   把`你的key`替换成你在 DeepSeek 官网申请的那串`sk-xxxx`开头的key
3. 保存

## 第三步：用 PyCharm 打开这个文件夹

1. 打开 PyCharm → File → Open → 选中 `agent_project` 这个文件夹
2. 打开 PyCharm 下方的 Terminal（终端），会自动定位到这个文件夹

## 第四步：安装依赖（只需要做一次）

在 PyCharm 的终端里输入：

```
pip install -r requirements.txt
```

## 第五步：启动项目

**注意：不要直接右键运行 main.py，没有用。** 因为 FastAPI 项目要交给 uvicorn 这个服务器程序来启动，不是靠直接执行 Python 文件启动的。

在终端里输入：

```
uvicorn main:app --reload
```

看到类似这样的提示就说明成功了：

```
Uvicorn running on http://127.0.0.1:8000
```

## 第六步：打开浏览器，测试基本功能

打开浏览器，访问：

```
http://127.0.0.1:8000
```

你会看到一个网页，里面有个输入框，粘贴你这周做的事情，点"生成报告"按钮就行。

**建议连续生成3-4次报告**（可以换不同的周期/内容），这样后面测试"跨周期诊断"功能时才有足够的历史数据。

## 第七步：测试新增的"跨周期诊断"功能

目前网页首页还没有加诊断功能的按钮，需要用 FastAPI 自带的接口文档页面来测试。

1. 浏览器访问：

```
http://127.0.0.1:8000/docs
```

2. 找到 `POST /api/diagnose`，点开，点右边的"Try it out"

3. 在请求体里填（`report_count`填你实际生成的报告数量，或者比这个数小的数字，最少填2）：

```json
{
  "report_count": 4
}
```

4. 点"Execute"，往下翻能看到返回结果，正常情况下会返回：
   - `patterns`：识别出的重复问题（如果历史记录里没有明显重复的问题，这里可能是空数组，属于正常情况）
   - `suggestions`：针对每个问题给出的具体建议
   - `overall_summary`：一句话总体诊断结论

## 如果出错了怎么办

- 提示 `command not found: uvicorn` → 说明依赖没装成功，回到第四步重新装一次
- 网页打开是空白或报错 → 看一下终端有没有红色报错文字，截图发给我
- 生成报告时显示"调用AI失败" → 大概率是 `.env` 里的 key 没填对，或者 DeepSeek 账户欠费/没绑定支付方式
- 调用 `/api/diagnose` 时返回"历史报告不足2条" → 先回到第六步多生成几次报告
- 终端一堆红色报错，提示 `ModuleNotFoundError: No module named 'agents'` 之类 → 检查一下`agents.py`、`models.py`、`storage.py`是不是和`main.py`放在了同一个文件夹里，不能放进子文件夹
